<%= form_for(@mineral_project) do |f| %>
  <% if @mineral_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mineral_project.errors.count, "error") %> prohibited this mineral_project from being saved:</h2>

      <ul>
      <% @mineral_project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
