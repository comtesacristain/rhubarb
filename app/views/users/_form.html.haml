- semantic_form_for @user do |f|
  - f.inputs do
    = f.input :username
    = f.input :email
    = f.input :role
    - f.buttons do
      =f.commit_button(:button_html => {:value => "Submit"})
      