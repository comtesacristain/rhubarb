<h1>Editing province</h1>

<% form_for(@province) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @province %> |
<%= link_to 'Back', provinces_path %>